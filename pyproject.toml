[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "Brabant-buurt-serious-game"
authors = [{name = "Naud Loomans", email = "naud@zenmo.com"}]
dynamic = ["version", "description"]
